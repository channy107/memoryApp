import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  commentContainer: {
    padding: 10,
    paddingBottom: 0,
  },
  comment: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 5,
  },
});

export default style;
